# Intrusion-detection-in-IoT-Network
Binary and multi-class intrusion detection on IoT network traffic (RT-IoT2022) using Logistic Regression, Naive Bayes, Random Forest, and KNN. A flow-aware train/test split was used to avoid leakage from near-duplicate synthetic attack traffic
